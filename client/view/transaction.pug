include header
body
    nav.navbar.navbar-dark.bg-dark.fixed-top.navbar-expand-lg
      a.navbar-brand(href='/')
        img.logo_s(src='/img/Logo.png')
        | TentERent
      button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarSupportedContent', aria-controls='navbarSupportedContent', aria-expanded='false', aria-label='Toggle navigation')
        span.navbar-toggler-icon
      .collapse.navbar-collapse#navbarSupportedContent
        ul.navbar-nav.mr-auto
          li.nav-item
            a.nav-link(href='/') Home
          li.nav-item
            a.nav-link(href='/item') Rentals
          li.nav-item
            a.nav-link(href='/contact') Contact
          li.nav-item
            a.nav-link(href='/aboutus') About Us
        ul#login_and_sign_up.navbar-nav
            li.nav-item.dropdown
                a#navbardrop.nav-link.dropdown-toggle(href='#', data-toggle='dropdown') #{username}
                .dropdown-menu
                    a.dropdown-item(href='/view_profile') View Profile
                    a.dropdown-item(href='/edit_profile') Edit Profile
                    a.dropdown-item(href='/transaction') Transaction
                    a.dropdown-item(href='/edit_profile') Logout
    .jumbotron#rental_banner
      .container
        h1.display-2 Transaction
    -var n = 0;
    while n < transac_id.length
        p #{transac_id[n]}
        p #{item_name[n]}
        p #{item_type[n]}
        p #{date_rented[n]}
        p #{date_due[n]}
        p #{amount[n]}
        p #{quantity[n]}
        p #{provider[n]}
        p #{approved[n]}
        p #{returend[n]}
include footer